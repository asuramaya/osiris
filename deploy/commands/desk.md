Read the operator's desk and render it. Call the osiris MCP tool `inbox` with project='operator' and peek=true (a glance must NEVER lease or settle the operator's mail — the count means "briefs the operator hasn't dismissed"). Render newest-first, compact: one block per brief — sender project + agent, age, then the body. If empty, say "desk clear".

SETTLE — only at the operator's word: if $ARGUMENTS contains "settle" or "clear" (or the operator explicitly says so), first render via peek as above, then settle exactly the ids you rendered with inbox(project='operator', ack=[ids]) and confirm the new desk count. Never settle on your own initiative; never settle ids you did not just show.
