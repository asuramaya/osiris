Check this project's mailbox. Call the osiris MCP tool `inbox` with peek=true first (mount with cwd + $CLAUDE_JOB_DIR if not mounted). Render each message compactly: id, sender, age, thread, body. Then remind in one dim line: reading with peek leases nothing — to take the mail, run inbox() without peek and SETTLE each handled message (send(reply_to=<id>) or inbox(ack=[ids])). If the operator asks to act on a message, do the non-peek read and settle properly.
