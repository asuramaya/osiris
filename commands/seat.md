Manage seats — one door composing existing verbs (decisions 4a3858e9/87457dc1, ruling 1a5eaf98), never new orchestration. Parse `$ARGUMENTS`: the first word is the verb, the rest are its own args. Full manual per verb, moved out of this file to keep it thin: call the osiris MCP tool `describe` with `table='seat'` for the verb list, or `table='seat:<verb>'` for one verb's full text — read it before running an unfamiliar verb.

Verbs: new, walk-in, mint, launch, resume, stop, move, bind-tree, heal-anchor, correct-agent-house, retire-agent, heal-seat-transcript, transition, retire, roster.

STANDING LAW (#102): never pick a winner on a disagreement (`pin_charter_agreement=='disagree'`, roster's own `conflict`/`near_misses`) — surface it with the repair verb named, never silently resolved. `'n/a'` is a valid state, never a problem.
