Wake the other half of your own `managed_by` pair (you manage them, or they manage you — never a peer, never cross-house; those route through a manager or the operator instead). `<handle>` from `$ARGUMENTS` is the target, the rest is the `message`. First call the osiris MCP tool `wake_preflight` with `target=<handle>` to answer wake()'s own gates before attempting one — report `status` if it is anything other than `resumable`/`fresh-heir-available` and stop there, surfacing the refusal verbatim rather than attempting the real call anyway. Otherwise call the `wake` MCP tool with `target=<handle>`, `message=<message>`. Print the returned result verbatim in a code block. No commentary, no re-rendering.
