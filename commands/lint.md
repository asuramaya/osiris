Audit the graph itself — report-only, never writes; heal with compensating events, never DELETE. Call the osiris MCP tool `graph_lint` (optionally `check` to page one finding type, `stale_days` for the obligation-staleness window) for contradiction/laundering/lineage-integrity/orphan/stale/attribution/phantom-twin/parallel-lives/duplicate-works_in/peer-silent/held-past-deadline findings. For the object SET itself (census by type/status, or `mode='buckets'` with `object_type` for per-object triage buckets: contradicted/duplicate_suspect/bulk_import/orphan/hub/stale/thin/normal), call the `triage` MCP tool instead. Print the returned result verbatim in a code block. No commentary, no re-rendering — findings are testimony to judge, not to summarize away.
