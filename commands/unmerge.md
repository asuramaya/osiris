Reverse a wrongful `/merge`. `$ARGUMENTS` gives `<dupe> <because...>` (the rest of the line is `because`; require both). Call the osiris MCP tool `unmerge` with `dupe`, `because` — DRY RUN IS THE DEFAULT (`execute` unset/False): show the returned plan first, and only call again with `execute=True` after the caller explicitly confirms it. Reversing an operator-blessed merge needs a fresh operator word in `because`, not a restatement of the original justification — surface that refusal verbatim if it fires. Print the returned result verbatim in a code block. No commentary, no re-rendering.
