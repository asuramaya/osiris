Open a new Thread — an unresolved question or next step for the next session to pick up. Call the osiris MCP tool `open_thread` with `summary` and `kind` (REQUIRED — 'obligation' for a duty an action just minted, 'question', 'task', or whatever genuinely fits; refuses rather than mint a kindless thread) plus `owner` (optional — a seat id/handle or 'operator'; defaults to the caller's own seat for an obligation, stays legitimately unowned for a general thread) and any of `repo`/`arc`/`resolves`/`assignee`/`branch`/`files_touched` the caller named. Print the returned receipt dict verbatim in a code block. No commentary, no re-rendering. To act on an EXISTING thread (resolve/annotate/reclassify) use `/thread` instead — this command only ever mints a new one.
