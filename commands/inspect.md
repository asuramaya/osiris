Inspect one graph object by `<ref>` (uuid, short id, canonical, or name) from `$ARGUMENTS`. Call the osiris MCP tool `dossier` with `object_ref=<ref>` for identity properties and the named relationship network (pass `want_relationships=True` only if the caller asks for every relationship row, not just the first 10). For merge/unmerge/split history on the same object, call the `object_events` MCP tool with `object_ref=<ref>` instead (optionally `event_type` to narrow). For an Agent's full succession lineage across generations, call the `succession_chain` MCP tool with `ref=<ref>`. With no `<ref>` at all, call the `candidates` MCP tool (this project's own unread miner guesses) instead — report-only, judge with `dispose()` directly, not this command. Print whichever result(s) the caller asked for verbatim in a code block. No commentary, no re-rendering.

`/inspect <ref>` (dossier only) is served by the hook without a model turn; falls through when arguments imply an act or no ref is given.
