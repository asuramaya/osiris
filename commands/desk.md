Read the operator's desk. Call the osiris MCP tool `inbox` with project='operator' (always peek-shaped, reading never leases — the count means "briefs not yet dismissed").

Render in band order, compact:
1. **NEEDS YOUR DECISION**, **BLOCKED ON YOUR HANDS**, **FYI** — one block per card: sender project + agent, age, body. `same_story`: "×N also: <projects>" dim line (`also` ids settle with the lead). `thread_folded`: note "(supersedes N earlier)".
2. **DIMMED** — one line each: headline + who dimmed it + the moot reason (still yours to dismiss).
3. **YOUR QUEUE** — owner='operator' open threads, one line.

Empty: say "desk clear".

SETTLE only at the operator's word: on "settle"/"clear" in `$ARGUMENTS` (or spoken), render first, then settle exactly the ids shown — fold `also`/`thread_folded` ids too, dimmed ids on "clear dimmed" — via inbox(project='operator', ack=[ids]); confirm the new count. Never settle unshown ids or on your own initiative. YOUR QUEUE closes via resolve_thread, never here.
