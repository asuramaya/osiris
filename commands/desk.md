DEFAULT (a glance, no $ARGUMENTS): call the osiris MCP tool `inbox` with project='operator', render='text' and print the returned `text` field verbatim in a code block — the backlog band first, then owed/letters, then needs_decision/needs_hands/fyi/dimmed/miner_guesses each as ONE COUNT LINE (thread 68f1bafa's own "backlog band first and briefs collapsed to one count line"), then your_queue itemized. No commentary.

SETTLE — only at the operator's word, and only when $ARGUMENTS asks for it ("settle", "clear", or the operator explicitly says so): the collapsed glance above deliberately drops the per-brief ids a settle needs, so re-call `inbox` with project='operator' WITHOUT render (the full structured bands: needs_decision/needs_hands/fyi cards, each with its own id, `same_story`'s `also` ids, `thread_folded` ids, `dimmed`). Render that shape compactly (one line per card: sender project + agent, age, body), then settle exactly the ids you just showed — including each fold's `also`/`thread_folded` ids and, if the operator says "clear dimmed", the dimmed ids — with inbox(project='operator', ack=[ids]) and confirm the new desk count. Scoped settles are honored: "settle the fyi band", "clear dimmed". Never settle on your own initiative; never settle ids you did not just show. Threads in your_queue are never settled from here — they close via resolve_thread when the work is actually done.
