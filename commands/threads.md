Call the osiris MCP tool `threads` with render='text' and print the returned `text` field verbatim in a code block. No commentary, no re-rendering.

Served by the hook without a model turn; falls through when arguments imply an act.
