Mint a seat as manager over one or more workers — self-managed, never a coordinator acting for someone else (operator 2026-09-07: "thoth cannot do it, it has to be self managed"). Parse `$ARGUMENTS`: the first token is `target`, the rest (up to `--because`/`because=`) are `workers`, the remainder is `because`. Call the osiris MCP tool `seat` with action='promote', `target`=<handle>, `workers`=[<handle>, ...], `because`=<str> and print the returned manifest dict verbatim in a code block. No commentary, no re-rendering. Refuses loudly if `because` is blank, if `target` is unknown, or if the caller is neither `target`'s own holder nor the operator — surface that refusal verbatim too, never retried silently.
