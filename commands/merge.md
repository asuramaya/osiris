Declare two labels of the same type one thing — append-only, nothing deleted. `$ARGUMENTS` gives `<dupe> <into> <evidence...>` (the rest of the line after the first two tokens is the `evidence` string; require all three, never guess a blank evidence). Call the osiris MCP tool `merge` with `dupe`, `into`, `evidence`, and `because`/`force` only if the caller named them. AGENT MERGES ARE ACTOR-GATED (operator or the scheduled reaper only) and a Person is NEVER auto-merged (constitution #1) — if it refuses on either ground, surface the refusal verbatim, do not retry with `force`. Print the returned receipt dict verbatim in a code block. No commentary, no re-rendering.
