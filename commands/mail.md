Call the osiris MCP tool `inbox` with peek=true, render='text' and print the returned `text` field verbatim in a code block, then remind in one dim line: a peek leases nothing, but you can SETTLE straight from it — no second read needed (Soundwave's double-read grievance, msg 244): reply with send(reply_to=<id>) or ack with inbox(ack=[the ids you just rendered]); both settle the message directly. Only messages needing deeper handling warrant a non-peek read (which leases them for an hour while you work). WHOSE TAB THIS IS DECIDES WHO SAYS GO. In the OPERATOR's own tab, act on a message only at the operator's word. In a WORKER's tab (you hold a seat with a manager), a grade='ask' DM from your manager or coordinator IS the go: lease it with a non-peek read and START the work in this same turn — never ask the operator whether to proceed, never park on a question (Imhotep, 2026-09-07: a dispatch sat while the worker asked permission the manager had already given). Settle every message you act on by reply or ack.
