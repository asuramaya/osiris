Check this project's mailbox. Call the osiris MCP tool `inbox` with peek=true first (mount with cwd + $CLAUDE_JOB_DIR if not mounted). Render each message compactly: id, sender, age, thread, body. Then remind in one dim line: a peek leases nothing, but you can SETTLE straight from it — no second read needed (Soundwave's double-read grievance, msg 244): reply with send(reply_to=<id>) or ack with inbox(ack=[the ids you just rendered]); both settle the message directly. Only messages needing deeper handling warrant a non-peek read (which leases them for an hour while you work). If the operator asks to act on a message, settle it properly by reply or ack.
