Call the osiris MCP tool `team` with render='text' and print the returned `text` field verbatim in a code block. No commentary, no re-rendering. If it errors because you manage nobody or hold no seat, say so plainly rather than falling back to `fleet`.
