Read or write the settings registry. With no `$ARGUMENTS`, call the osiris MCP tool `settings` with `action='list'` (every setting, its `value`/`live` state, secrets redacted). With `<key>`, call `settings` with `action='get'`, `key=<key>`. With `set <key> <value>`, call `settings` with `action='write'`, `key`, `value`, `because` (required unless the setting opts out) — each setting's own `authority` decides who may write it without a manager/ruling citation. For the backup vault path, offbox repositories, or the five backup-timer schedules, call the `backup_settings` MCP tool instead: `action='get'` needs nothing else; `action='write'` takes whichever of `vault_path`/`timer_schedules`/`offbox_repositories` were named (partial update) plus `because` (operator writes freely, anyone else cites a standing `ruling` naming 'backup_settings'). Print the result verbatim in a code block. No commentary, no re-rendering.
