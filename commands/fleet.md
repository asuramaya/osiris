If you hold a seat that manages others, call `team` with render='text' and print its `text` field verbatim in a code block — that IS your fleet (thread 68f1bafa: "/fleet as team for a manager and self otherwise"). If `team` errors because you manage nobody (or you hold no seat), fall back to the full fleet view: call the osiris MCP tool `fleet` and render its `tree` verbatim in a code block (● live / ○ historical), then call `fleet_digest` with hours=24 and give ONE summary line from its `summary` (agents, unresolved, swapped, conversations, operator_unread) plus the danger map rows if any agents are swapped. Compact — no tables beyond the tree, no advice.
