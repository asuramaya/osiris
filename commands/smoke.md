Deploy-time liveness — walks every chrome route and runs one real query over THIS server's own pool, the only thing that catches a wrong-pool bug a static gate cannot. Call the osiris MCP tool `smoke` with no arguments. Print the returned result verbatim in a code block. `ok=false` names exactly which surface failed — surface that verbatim, don't paraphrase it away. Run this right after a restart, not just once at boot. No commentary, no re-rendering.
