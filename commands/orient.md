Get your bearings. Call the osiris MCP tool `orient` (mount first if not mounted) — an explicit `project` overrides your own mount; unmounted with neither gives the whole-fleet briefing. Pass `verbose=True` only if the caller wants full prose back rather than the terse default. If the result carries a live succession-note pointer to an unacknowledged handoff, call the `handoff_briefing` MCP tool with `repo=<your project>` to compile it from the graph, then — once the caller has actually judged it, never automatically — call the `ack_handoff` MCP tool with `ref=<the handoff's id>` to retire the marker. Print the returned result(s) verbatim in a code block. No commentary, no re-rendering.
