Call the osiris MCP tool `backlog` with render='text' and print the returned `text` field verbatim in a code block. No commentary, no re-rendering. Pass all_projects=True only if the human explicitly asks to see every project, not just the current one. Pass fleet=True instead (thread 8608, THE BACKLOG BAND) if the human asks who is carrying the backlog fleet-wide, per seat rather than per project — fleet=True takes priority over all_projects when both would apply.
