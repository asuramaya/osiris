The operator's membrane into the autonomous fleet. With no `$ARGUMENTS`, call the osiris MCP tool `fleet_digest` with no `hours` (watermark mode — what's new since the operator last looked) and print roster/health, activity, the danger map, laundering flags, spend, and obligation_pressure verbatim in a code block. Glancing is a PEEK — it never advances the watermark. Pass `hours=<n>` only if the caller asks for an explicit rolling window instead. Only call again with `mark_seen=True` when the operator's own word ("seen"/"mark seen"/"clear") says the read is done — never on your own initiative. No commentary, no re-rendering.

A bare `/digest` (no arguments) is served by the hook without a model turn; falls through when arguments imply an act (including "seen"/"mark_seen"/"clear").
