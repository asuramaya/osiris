Act on an existing Thread. With a `ref` and an `action` (resolve/annotate/correct_summary/reclassify) plus that action's own params (`because`/`artifact`/`note`/`corrected_summary`/`kind`/`owner`/`arc`), call the osiris MCP tool `thread`. To open a NEW thread instead, call `open_thread` with a `summary`. Either way, print the returned receipt dict verbatim in a code block. No commentary, no re-rendering.
